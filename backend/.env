# Backend configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=Kunal@2027
DB_NAME=netrasync

# SMTP config for OTP emails (optional - will use dummy if not configured)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@example.com
SMTP_PASS=your_app_password
SMTP_FROM=NetraSync <your_email@example.com>
